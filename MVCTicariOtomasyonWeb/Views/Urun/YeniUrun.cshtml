@model MVCTicariOtomasyonWeb.Models.sınıflar.Urun
@{
    Layout = "~/Views/Shared/AdminLayout.cshtml";
    ViewData["Title"] = "Yeni Ürün Ekle";
}

<div class="container-fluid px-4 mt-4">

    <!-- 🔹 TempData mesajları -->
    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="fas fa-check-circle me-2"></i>@TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Kapat"></button>
        </div>
    }
    @if (TempData["DbError"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="fas fa-times-circle me-2"></i>@TempData["DbError"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Kapat"></button>
        </div>
    }
    @if (TempData["ModelErrors"] != null)
    {
        <div class="alert alert-warning alert-dismissible fade show" role="alert">
            <i class="fas fa-exclamation-triangle me-2"></i>@TempData["ModelErrors"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Kapat"></button>
        </div>
    }

    <!-- 🔹 Kart -->
    <div class="card shadow-sm border-0 rounded-3">
        <div class="card-header bg-gradient-primary text-white d-flex justify-content-between align-items-center">
            <h5 class="mb-0"><i class="fas fa-plus-circle me-2"></i>Yeni Ürün Ekle</h5>
            <a asp-controller="Urun" asp-action="Index" class="btn btn-light btn-sm fw-bold">
                <i class="fas fa-arrow-left me-1"></i>Geri Dön
            </a>
        </div>

        <!-- 🔹 Form -->
        <div class="card-body bg-light">
            <form method="post" asp-action="YeniUrun" enctype="multipart/form-data">
                @Html.AntiForgeryToken()
                <div asp-validation-summary="All" class="text-danger mb-3"></div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label class="form-label fw-bold">Ürün Adı</label>
                        <input asp-for="UrunAd" class="form-control" placeholder="Ürün adı giriniz..." required />
                    </div>
                    <div class="col-md-6 mb-3">
                        <label class="form-label fw-bold">Marka</label>
                        <input asp-for="Marka" class="form-control" placeholder="Marka giriniz..." required />
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-4 mb-3">
                        <label class="form-label fw-bold">Stok</label>
                        <input asp-for="Stok" type="number" class="form-control" min="0" />
                    </div>
                    <div class="col-md-4 mb-3">
                        <label class="form-label fw-bold">Alış Fiyatı</label>
                        <input asp-for="AlisFiyat" type="number" step="0.01" class="form-control" />
                    </div>
                    <div class="col-md-4 mb-3">
                        <label class="form-label fw-bold">Satış Fiyatı</label>
                        <input asp-for="SatisFiyat" type="number" step="0.01" class="form-control" />
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-4 mb-3">
                        <label class="form-label fw-bold">Durum</label>
                        <select asp-for="Durum" class="form-select">
                            <option value="true">Aktif</option>
                            <option value="false">Pasif</option>
                        </select>
                    </div>
                    <div class="col-md-4 mb-3">
                        <label class="form-label fw-bold">Kategori</label>
                        <select asp-for="KategoriId" asp-items="ViewBag.Kategoriler" class="form-select">
                            <option value="">Kategori Seçiniz</option>
                        </select>
                    </div>
                    <div class="col-md-4 mb-3">
                        <label class="form-label fw-bold">Ürün Görseli</label>
                        <input type="file" name="UrunGorsel" class="form-control" accept="image/*" />
                    </div>
                </div>

                <div class="text-end">
                    <button type="submit" class="btn btn-success fw-bold">
                        <i class="fas fa-save me-1"></i>Kaydet
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
